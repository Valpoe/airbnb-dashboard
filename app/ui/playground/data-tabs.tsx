// import { slowFetch } from '@/app/lib/database';

export default async function DataTabs() {
  // const fetchSlowData = await slowFetch();

  return (
    <div>
      {/* {fetchSlowData.map((listing) => (
        <div key={listing.id}>{listing.internal_name}</div>
      ))} */}
    </div>
  );
}
